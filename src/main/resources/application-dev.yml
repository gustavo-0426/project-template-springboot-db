server:
  port: ${SERVER_PORT}

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=disable
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD} 
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

api:
  version: v1

springdoc:
  swagger-ui:
    path: /${api.version}/TEMPLATE_SERVICE_NAME/           # http://{host}:{port}/{api.version}/template/swagger-ui/index.html
  api-docs:
    path: /${api.version}/TEMPLATE_SERVICE_NAME/api-docs   # http://{host}:{port}/{api.version}/template/api-docs

logging:
  level:
    org:
      springdoc: DEBUG